# Prerequisites

## Required

- **Cortex Code CLI** v1.0.70+
- **Snowflake connection** named `default` with access to:
  - ACCOUNT_USAGE views (for drift detection)
  - INFORMATION_SCHEMA (for artifact inspection)

## Optional

- **cortex search docs** access — used by release-change-monitor for docs queries
