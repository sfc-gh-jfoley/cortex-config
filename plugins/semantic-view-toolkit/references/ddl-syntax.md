# DDL Syntax Reference (DEPRECATED)

> ⚠️ **This reference is outdated and uses a grammar that does not match current Snowflake semantic view DDL.**
>
> Use the canonical reference instead:
> **`skills/sv-ddl/reference/ddl_syntax.md`**
>
> That file contains the complete, current CREATE SEMANTIC VIEW grammar including:
> `AI_VERIFIED_QUERIES`, `AI_SQL_GENERATION`, `AI_QUESTION_CATEGORIZATION`, `LABELS = (FILTER)`,
> `PRIVATE/PUBLIC` visibility, window function metrics, NON ADDITIVE BY, DISTINCT RANGE BETWEEN,
> ASOF joins, named relationships, and the `WITH SYNONYMS = (...)` / `COMMENT =` column syntax.
>
> This file is kept as a placeholder to prevent broken links from legacy skill references.
