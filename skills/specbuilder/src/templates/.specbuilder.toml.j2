# SpecBuilder project configuration

[project]
name = "{{ project_name }}"
mode = "{{ mode }}"
spec_dir = "{{ spec_dir }}"
protected_dirs = {{ protected_dirs | tojson }}

[quality]
profile = "{{ mode }}"
